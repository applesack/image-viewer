import { AxisType, Placement } from "../../common/constants.ts";

export interface SlideBoxProps {
  axis?: AxisType
  overlay?: boolean,
  placement?: Placement
}

export interface SlideBoxInstance {

}
